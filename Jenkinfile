pipeline {
    agent any

    stages {
        stage('Clone') {
            steps {
                echo 'Cloning Repository'
            }
        }

        stage('Build') {
            steps {
                echo 'Building Website'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying Website'
            }
        }
    }
}
